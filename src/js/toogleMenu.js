export const handlerToogleMenu = (function () {
  console.log('handlerToogleMenu');
  
  alert( 'Спасибо!' );

// var menuA = document.getElementById("menu");
  
// menu.addEventListener("click", alert( 'Спасибо!' ));

// menu.removeEventListener("click", handlerToogleMenu)
});

// export default handlerToogleMenu;
